<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHealthReminder">
        <activity
            android:name=".features.Search.SearchActivity"
            android:exported="false" />
        <activity
            android:name=".features.Search.NoResultsActivity"
            android:exported="false" />
        <activity
            android:name=".features.welcome.WelcomeActivity2"
            android:exported="false" />
        <activity
            android:name=".features.auth.signup.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".features.auth.signIn.SignInActivity"
            android:exported="false" />


        <activity
            android:name=".features.Profileinfo.reports.ProfileActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".features.auth.forget_password.ForgetPasswordActivity"
            android:exported="false" />
    </application>

</manifest>